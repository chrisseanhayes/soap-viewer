<?xml version="1.0" encoding="utf-8"?>
<node id="Client" next="AppClient" label="Client Application Environment" nodeType="Compute" diagramClass="osiLayer">
  <title>Client Application Environment</title>
  <infoCard title="Overview">
    The entire local execution environment where the client application resides.
    This boundary encloses everything that happens in memory before bytes are
    placed onto the network.
  </infoCard>
  <briefcaseCard title="Handled By">
    &lt;strong&gt;The Developer &amp;amp; Client OS:&lt;/strong&gt; Developers manage
    the application and proxy, while the runtime/OS handles memory and threading.
  </briefcaseCard>
  <lightningCard title="Developer Impact">
    Errors inside this boundary are usually local exceptions
    (e.g. Type mismatch, NullReference), which are much faster and easier to
    debug than network-level faults.
  </lightningCard>
  <arrowRightCard title="Next Steps">
    Coordinates local proxies to serialize data and pass it to the transport layer.
  </arrowRightCard>
  <arrowsCard title="Branching &amp; Flow">
    Local validation failures occur entirely within this zone, saving a
    round-trip to the server.
  </arrowsCard>
  <lightbulbCard title="Developer Considerations">
    Developers should consider memory constraints, local dependency injection,
    and overall client configuration before engaging the <def id="SOAP">SOAP</def> service.
  </lightbulbCard>
  <bigPicture>
    <role>The Caller — Where the <def id="RPC">RPC</def> Illusion Begins</role>
    <summary>
      This is the origin of every <def id="SOAP">SOAP</def> request. The entire premise of <def id="RPC">RPC</def>
      (Remote Procedure Call) is that calling a function on a distant server
      should look and feel identical to calling a local function. That illusion
      is constructed here. The developer writes
      &lt;code&gt;paymentService.processCharge(card, amount)&lt;/code&gt; — no HTTP,
      no <def id="XML">XML</def>, no network awareness — just a method call. The generated <def id="Proxy">Proxy</def>
      class silently intercepts it and begins the long journey across the network.
    </summary>
    <whyItExists>
      Without this abstraction, every developer would have to manually construct
      <def id="XML">XML</def> strings, open HTTP connections, set headers, handle timeouts, and parse
      responses. <def id="RPC">RPC</def> frameworks exist to eliminate that accidental complexity so
      developers can focus on business logic, not networking plumbing.
    </whyItExists>
    <journeyStep>
      Step 1 of 5: The intent is formed. A local method call is made.
      The network has not yet been touched.
    </journeyStep>
  </bigPicture>
  <definitionPills>
    <definitionPill id="Compute"/>
  </definitionPills>
  <node id="AppClient" next="EdgeInitiatesRequest" nodeType="ApplicationCode">
    <diagram>
      <nodeLabel>Client Application</nodeLabel>
      <classes>
        <class name="process"/>
        <class name="clickable"/>
      </classes>
      <edges direction="out">
        <edge to="Proxy" label="Initiates Request" type="solid-arrow" edgeKey="EdgeInitiatesRequest" next="Proxy">
          <sidebar>
            <title>Action: Initiates Request</title>
            <infoCard title="Overview">The client application calls a method on the generated <def id="Proxy">Proxy</def> object, passing in native programming objects (e.g., a Customer object or a string ID).</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The Developer:&lt;/strong&gt; Calls the local method and provides the arguments.</briefcaseCard>
            <lightningCard title="Developer Impact">Seems like a local function call, but acts as the gateway to a remote network operation.</lightningCard>
            <lightbulbCard title="Developer Considerations">The application thread will typically block here waiting for a response unless asynchronous methods are used.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="ApplicationCode"/>
          </definitionPills>
        </edge>
      </edges>
      <edges direction="in">
        <edge from="Deserialize2" label="Returns Result / Throws Fault" type="solid-arrow" edgeKey="EdgeReturnsResult">
          <sidebar>
            <title>Action: Returns Result / Throws Fault</title>
            <infoCard title="Overview">The final step of the round trip. The <def id="SOAP">SOAP</def> framework hands the deserialized result back to the application, completing the remote method call. If a <def id="SOAP">SOAP</def> Fault was in the response, a native exception is thrown instead.</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library &amp;amp; Developer:&lt;/strong&gt; The library completes the call; the developer's code receives the return value or catches the exception.</briefcaseCard>
            <lightningCard title="Developer Impact">From the developer's perspective, the entire network round-trip is invisible — they see only a return value or an exception, exactly as if they had called a local method.</lightningCard>
            <lightbulbCard title="Developer Considerations">This is why developers must always wrap proxy calls in try/catch. Both the happy path and the fault path emerge from this single point.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="ApplicationCode"/>
          </definitionPills>
        </edge>
      </edges>
    </diagram>
    <sidebar>
      <title>1. Client Application</title>
      <infoCard title="Overview">
        The originating software system that needs to consume a remote <def id="SOAP">SOAP</def> service
        to fetch data or trigger an action.
      </infoCard>
      <arrowRightCard title="Next Steps">
        Passes arguments to the locally instantiated <def id="Proxy">Proxy</def> class.
      </arrowRightCard>
      <arrowsCard title="Branching &amp; Flow">
        If the data provided to the method is invalid locally, the application
        throws an exception before the network is even engaged.
      </arrowsCard>
      <briefcaseCard title="Handled By">
        &lt;strong&gt;The Developer:&lt;/strong&gt; It's the developer's responsibility
        to know this and implement this.
      </briefcaseCard>
    </sidebar>
    <languages>
      <language id="java" label="Java">
        <lightningCard title="Developer Impact">
          Developers write standard local method calls
          (e.g., `paymentService.processCharge(...)`).
          They interact with strongly typed Java objects.
        </lightningCard>
        <codeSnippet>
PaymentService proxy = new PaymentService();
boolean success = proxy.processCharge("1234-5678", 50.00);
        </codeSnippet>
      </language>
      <language id="dotnet" label=".NET / C#">
        <lightningCard title="Developer Impact">
          Developers write standard local method calls
          (e.g., `paymentService.ProcessCharge(...)`).
          They interact with strongly typed C# objects.
        </lightningCard>
        <codeSnippet>
var proxy = new PaymentServiceClient();
bool success = proxy.ProcessCharge("1234-5678", 50.00);
        </codeSnippet>
      </language>
      <language id="python" label="Python">
        <lightningCard title="Developer Impact">
          Developers write standard local method calls
          (e.g., `client.service.processCharge(...)`).
          They pass standard Python data structures like dicts.
        </lightningCard>
        <codeSnippet>
result = client.service.processCharge("1234-5678", 50.00)
        </codeSnippet>
      </language>
    </languages>
    <definitionPills>
      <definitionPill id="ApplicationCode"/>
    </definitionPills>
  </node>
  <node id="Proxy" next="EdgeGeneratesXML" nodeType="LibraryCode">
    <diagram>
      <nodeLabel>Client <def id="Proxy">Proxy</def> / Stub</nodeLabel>
      <nodeId>
        <def id="Proxy">Proxy</def>
      </nodeId>
      <classes>
        <class name="process"/>
        <class name="clickable"/>
      </classes>
      <edges direction="out">
        <edge to="Serialize" label="Generates XML" type="solid-arrow" edgeKey="EdgeGeneratesXML" next="Serialize">
          <sidebar>
            <title>Action: Generates <def id="XML">XML</def></title>
            <infoCard title="Overview">The proxy intercepts the method call and invokes the <def id="SOAP">SOAP</def> serialization engine to convert the native objects into a strict <def id="XML">XML</def> structure according to the <def id="WSDL">WSDL</def>.</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library:&lt;/strong&gt; Automatically performs the object-to-<def id="XML">XML</def> mapping.</briefcaseCard>
            <lightningCard title="Developer Impact">Saves the developer from writing manual <def id="XML">XML</def> string concatenation and parsing logic.</lightningCard>
            <lightbulbCard title="Developer Considerations">If the objects contain circular references or unsupported data types, this step will throw a serialization exception.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="LibraryCode"/>
          </definitionPills>
        </edge>
      </edges>
      <edges direction="in">
        <edge from="AppClient" label="Initiates Request" type="solid-arrow" edgeKey="EdgeInitiatesRequest" next="Proxy">
          <sidebar>
            <title>Action: Initiates Request</title>
            <infoCard title="Overview">The client application calls a method on the generated <def id="Proxy">Proxy</def> object, passing in native programming objects (e.g., a Customer object or a string ID).</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The Developer:&lt;/strong&gt; Calls the local method and provides the arguments.</briefcaseCard>
            <lightningCard title="Developer Impact">Seems like a local function call, but acts as the gateway to a remote network operation.</lightningCard>
            <lightbulbCard title="Developer Considerations">The application thread will typically block here waiting for a response unless asynchronous methods are used.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="ApplicationCode"/>
          </definitionPills>
        </edge>
      </edges>
    </diagram>
    <sidebar>
      <title>2. Client <def id="Proxy">Proxy</def> / Stub</title>
      <infoCard title="Overview">A piece of code acting as a local representative of the remote service, intercepting method calls and packaging the parameters for transmission.</infoCard>
      <arrowRightCard title="Next Steps">Takes language-specific objects and passes them to the Serialization framework.</arrowRightCard>
      <arrowsCard title="Branching &amp; Flow">The proxy enforces type safety. If the developer tries to call a method not defined in the <def id="WSDL">WSDL</def> contract, it fails (at compile time for Java/.NET, or runtime for Python).</arrowsCard>
      <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library:&lt;/strong&gt; It's the library's responsibility to transform/perform the task.</briefcaseCard>
      <lightbulbCard title="Developer Considerations">Developers need to consider network latency. While proxy methods appear as local calls, they incur significant network overhead. Calling them inside tight loops can lead to severe performance bottlenecks (the <def id="NPlus1Problem">N+1 problem</def>).</lightbulbCard>
    </sidebar>
    <languages>
      <language id="java" label="Java">
        <lightningCard title="Developer Impact">Developers rarely write this manually. They use tooling like `wsimport` (JAX-WS) or `wsdl2java` (Apache CXF) to point at a <def id="WSDL">WSDL</def> URL and generate these classes.</lightningCard>
        <codeSnippet>
// Auto-generated interface stub
@WebService(targetNamespace = "http://api.example.com/")
public interface PaymentService {
    @WebMethod
    boolean processCharge(String card, double amt);
}
        </codeSnippet>
      </language>
      <language id="dotnet" label=".NET / C#">
        <lightningCard title="Developer Impact">Developers rarely write this manually. They use tooling like `svcutil` (WCF) or `dotnet-svcutil` to point at a <def id="WSDL">WSDL</def> URL and generate these C# classes.</lightningCard>
        <codeSnippet>
// Auto-generated interface stub
[System.ServiceModel.ServiceContractAttribute]
public interface IPaymentService {
    [System.ServiceModel.OperationContractAttribute]
    bool ProcessCharge(string card, double amt);
}
        </codeSnippet>
      </language>
      <language id="python" label="Python">
        <lightningCard title="Developer Impact">Developers use libraries like Zeep, which dynamically parses the <def id="WSDL">WSDL</def> at runtime and generates proxy objects in memory, embracing Python's dynamic typing.</lightningCard>
        <codeSnippet>
# Zeep parses the <def id="WSDL">WSDL</def> dynamically at runtime
# No physical stub classes are generated.
client = Client(wsdl="http://api.example.com/pay?wsdl")
        </codeSnippet>
      </language>
    </languages>
    <definitionPills>
      <definitionPill id="LibraryCode"/>
    </definitionPills>
  </node>
  <node id="Serialize" next="OSILayer7" nodeType="LibraryCode">
    <diagram>
      <nodeLabel>Serialize to <def id="SOAP">SOAP</def> Message</nodeLabel>
      <classes>
        <class name="process"/>
        <class name="clickable"/>
      </classes>
      <edges direction="out">
        <edge to="TransportProtocol" label="Wraps in HTTP/SMTP" type="solid-arrow" edgeKey="EdgeWrapsInHTTP" next="TransportProtocol">
          <sidebar>
            <title>Action: Wraps in HTTP/SMTP</title>
            <infoCard title="Overview">The raw <def id="XML">XML</def> string is wrapped in a transport protocol envelope. For HTTP, this means creating an HTTP POST request with specific headers like 'Content-Type: text/xml' and 'SOAPAction'.</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The Transport Protocol (HTTP Client):&lt;/strong&gt; Prepares the message for the network stack.</briefcaseCard>
            <lightningCard title="Developer Impact">The HTTP status codes (200 OK, 500 Internal Server Error) often dictate the initial success or failure of the transmission.</lightningCard>
            <lightbulbCard title="Developer Considerations">Custom headers (like Authorization for Basic Auth) must be injected before this step is finalized.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="OSHardwareCode"/>
          </definitionPills>
        </edge>
      </edges>
      <edges direction="in">
        <edge from="Proxy" label="Generates XML" type="solid-arrow" edgeKey="EdgeGeneratesXML" next="Serialize">
          <sidebar>
            <title>Action: Generates <def id="XML">XML</def></title>
            <infoCard title="Overview">The proxy intercepts the method call and invokes the <def id="SOAP">SOAP</def> serialization engine to convert the native objects into a strict <def id="XML">XML</def> structure according to the <def id="WSDL">WSDL</def>.</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library:&lt;/strong&gt; Automatically performs the object-to-<def id="XML">XML</def> mapping.</briefcaseCard>
            <lightningCard title="Developer Impact">Saves the developer from writing manual <def id="XML">XML</def> string concatenation and parsing logic.</lightningCard>
            <lightbulbCard title="Developer Considerations">If the objects contain circular references or unsupported data types, this step will throw a serialization exception.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="LibraryCode"/>
          </definitionPills>
        </edge>
      </edges>
    </diagram>
    <sidebar>
      <title>3. Serialize to <def id="SOAP">SOAP</def> Message</title>
      <infoCard title="Overview">The framework's engine converts the application's native in-memory objects into a structured <def id="SOAP">SOAP</def> <def id="XML">XML</def> document format according to <def id="XML">XML</def> Schema rules.</infoCard>
      <arrowRightCard title="Next Steps">Passes the resulting raw <def id="XML">XML</def> string to the Transport Protocol layer.</arrowRightCard>
      <arrowsCard title="Branching &amp; Flow">If serialization fails (e.g., encountering a circular reference), a runtime serialization exception is thrown.</arrowsCard>
      <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library:&lt;/strong&gt; It's the library's responsibility to transform/perform the task.</briefcaseCard>
      <lightbulbCard title="Developer Considerations">Be mindful of object graph sizes. Serializing extremely large objects or lists can cause massive memory spikes and OutOfMemory errors.</lightbulbCard>
    </sidebar>
    <languages>
      <language id="java" label="Java">
        <lightningCard title="Developer Impact">Usually a "black box" handled by frameworks like JAX-WS or Apache CXF. Devs intervene here to configure <def id="XML">XML</def> namespace mappings or inject interceptors.</lightningCard>
        <codeSnippet>
// Injecting a custom interceptor during serialization
BindingProvider bp = (BindingProvider) proxy;
List&lt;Handler&gt; handlers = new ArrayList&lt;&gt;();
handlers.add(new MySecurityHandler());
bp.getBinding().setHandlerChain(handlers);
        </codeSnippet>
      </language>
      <language id="dotnet" label=".NET / C#">
        <lightningCard title="Developer Impact">Usually a "black box" handled by the Windows Communication Foundation (WCF). Devs use behaviors and message inspectors to manipulate headers.</lightningCard>
        <codeSnippet>
// Injecting an endpoint behavior in WCF
proxy.Endpoint.EndpointBehaviors.Add(
    new MyLoggingBehavior()
);
        </codeSnippet>
      </language>
      <language id="python" label="Python">
        <lightningCard title="Developer Impact">Handled under the hood by Zeep (using lxml). Developers can pass standard Python dictionaries, and Zeep securely maps them to the required <def id="XML">XML</def> Schema.</lightningCard>
        <codeSnippet>
from zeep.plugins import HistoryPlugin
# Zeep plugins can inspect/modify the <def id="XML">XML</def> tree
history = HistoryPlugin()
client = Client(wsdl, plugins=[history])
        </codeSnippet>
      </language>
    </languages>
    <definitionPills>
      <definitionPill id="LibraryCode"/>
    </definitionPills>
  </node>
  <node id="ClientTransport" next="EdgeDeserializesResponseXML" nodeType="LibraryCode">
    <diagram>
      <nodeLabel>Client Transport Receives</nodeLabel>
      <classes>
        <class name="process"/>
        <class name="clickable"/>
      </classes>
      <edges direction="out">
        <edge to="Deserialize2" label="Deserializes Response XML" type="solid-arrow" edgeKey="EdgeDeserializesResponseXML" next="Deserialize2">
          <sidebar>
            <title>Action: Deserializes Response <def id="XML">XML</def></title>
            <infoCard title="Overview">The client <def id="SOAP">SOAP</def> library extracts the <def id="XML">XML</def> from the HTTP response body and converts it back into native language objects (or throws a fault exception).</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library:&lt;/strong&gt; Automatically handles <def id="XML">XML</def> parsing and object mapping.</briefcaseCard>
            <lightningCard title="Developer Impact">The developer receives the result as a native object — completely unaware of the <def id="XML">XML</def> that was transmitted.</lightningCard>
            <lightbulbCard title="Developer Considerations">Very large response bodies cause proportionally large memory allocations during deserialization. Streaming parsers can mitigate this for list-type responses.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="LibraryCode"/>
          </definitionPills>
        </edge>
      </edges>
      <edges direction="in">
        <edge from="TCP" label="Delivers Response" type="solid-arrow" edgeKey="EdgeDeliversResponse" next="ClientTransport">
          <sidebar>
            <title>Action: Delivers Response</title>
            <infoCard title="Overview">The TCP/IP stack reassembles the response packets and delivers the complete HTTP response to the client process. The client transport layer hands it to the <def id="SOAP">SOAP</def> library.</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The OS Network Stack:&lt;/strong&gt; Handles packet ordering, retransmission, and final delivery.</briefcaseCard>
            <lightningCard title="Developer Impact">Transparent to developers, but contributes to the total round-trip time the application experiences.</lightningCard>
            <lightbulbCard title="Developer Considerations">If packets are lost on the return path, TCP retransmits them automatically — adding latency to the perceived response time.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="Network"/>
            <definitionPill id="OSHardwareCode"/>
          </definitionPills>
        </edge>
      </edges>
    </diagram>
    <sidebar>
      <title>9. Client Transport Receives</title>
      <infoCard title="Overview">The client-side HTTP client layer receives the HTTP response from the network stack. It checks the HTTP status code and passes the response body (the <def id="XML">XML</def>) to the <def id="SOAP">SOAP</def> deserialization layer.</infoCard>
      <arrowRightCard title="Next Steps">Receives the reassembled HTTP response from the OS networking stack and passes the <def id="XML">XML</def> body to the client <def id="SOAP">SOAP</def> library for parsing.</arrowRightCard>
      <arrowsCard title="Branching &amp; Flow">If the HTTP status is a transport-level error (e.g., 503 Service Unavailable, timeout), the <def id="SOAP">SOAP</def> library throws a transport exception before attempting to parse any <def id="XML">XML</def>.</arrowsCard>
      <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library &amp;amp; Network Layer:&lt;/strong&gt; The OS handles packet reassembly; the <def id="SOAP">SOAP</def> framework handles the HTTP layer.</briefcaseCard>
      <lightningCard title="Developer Impact">Generally handled by the <def id="SOAP">SOAP</def> framework, but developers must configure timeouts to avoid threads blocking indefinitely waiting for a response that will never arrive.</lightningCard>
      <lightbulbCard title="Developer Considerations">Developers should configure both connection and read timeouts. An unreachable server that silently drops packets will cause an indefinitely blocked thread without a read timeout.</lightbulbCard>
    </sidebar>
    <definitionPills>
      <definitionPill id="LibraryCode"/>
    </definitionPills>
  </node>
  <node id="Deserialize2" next="EdgeReturnsResult" nodeType="LibraryCode">
    <diagram>
      <nodeLabel>Deserialize Response</nodeLabel>
      <classes>
        <class name="process"/>
        <class name="clickable"/>
      </classes>
      <edges direction="out">
        <edge to="AppClient" label="Returns Result / Throws Fault" type="solid-arrow" edgeKey="EdgeReturnsResult">
          <sidebar>
            <title>Action: Returns Result / Throws Fault</title>
            <infoCard title="Overview">The final step of the round trip. The <def id="SOAP">SOAP</def> framework hands the deserialized result back to the application, completing the remote method call. If a <def id="SOAP">SOAP</def> Fault was in the response, a native exception is thrown instead.</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library &amp;amp; Developer:&lt;/strong&gt; The library completes the call; the developer's code receives the return value or catches the exception.</briefcaseCard>
            <lightningCard title="Developer Impact">From the developer's perspective, the entire network round-trip is invisible — they see only a return value or an exception, exactly as if they had called a local method.</lightningCard>
            <lightbulbCard title="Developer Considerations">This is why developers must always wrap proxy calls in try/catch. Both the happy path and the fault path emerge from this single point.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="ApplicationCode"/>
          </definitionPills>
        </edge>
      </edges>
      <edges direction="in">
        <edge from="ClientTransport" label="Deserializes Response XML" type="solid-arrow" edgeKey="EdgeDeserializesResponseXML" next="Deserialize2">
          <sidebar>
            <title>Action: Deserializes Response <def id="XML">XML</def></title>
            <infoCard title="Overview">The client <def id="SOAP">SOAP</def> library extracts the <def id="XML">XML</def> from the HTTP response body and converts it back into native language objects (or throws a fault exception).</infoCard>
            <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library:&lt;/strong&gt; Automatically handles <def id="XML">XML</def> parsing and object mapping.</briefcaseCard>
            <lightningCard title="Developer Impact">The developer receives the result as a native object — completely unaware of the <def id="XML">XML</def> that was transmitted.</lightningCard>
            <lightbulbCard title="Developer Considerations">Very large response bodies cause proportionally large memory allocations during deserialization. Streaming parsers can mitigate this for list-type responses.</lightbulbCard>
          </sidebar>
          <definitionPills>
            <definitionPill id="LibraryCode"/>
          </definitionPills>
        </edge>
      </edges>
    </diagram>
    <sidebar>
      <title>10. Deserialize Response</title>
      <infoCard title="Overview">The client <def id="SOAP">SOAP</def> library parses the response <def id="XML">XML</def>. On success, it maps the <def id="XML">XML</def> elements back into native language objects and returns them. On failure (<def id="SOAP">SOAP</def> Fault present), it parses the fault codes and throws a language-native exception.</infoCard>
      <arrowRightCard title="Next Steps">Returns the deserialized result object to the calling application, completing the round trip. On fault, throws an exception that propagates up to the application code.</arrowRightCard>
      <arrowsCard title="Branching &amp; Flow">Success path → native return object handed back to the developer's code. Fault path → <def id="SOAP">SOAP</def> Fault parsed and thrown as a native exception (e.g., WebServiceException in Java, SoapException in .NET).</arrowsCard>
      <briefcaseCard title="Handled By">&lt;strong&gt;The <def id="SOAP">SOAP</def> Library:&lt;/strong&gt; Automatically deserializes the <def id="XML">XML</def> response; the developer only interacts with the resulting native object or exception.</briefcaseCard>
      <lightningCard title="Developer Impact">This is the last step in the <def id="SOAP">SOAP</def> call. The developer's original blocking method call finally unblocks and receives either a return value or an exception.</lightningCard>
      <lightbulbCard title="Developer Considerations">Developers must implement try/catch around proxy calls to handle <def id="SOAP">SOAP</def> Faults. Uncaught exceptions from this step will crash the calling thread.</lightbulbCard>
    </sidebar>
    <definitionPills>
      <definitionPill id="LibraryCode"/>
    </definitionPills>
  </node>
</node>
